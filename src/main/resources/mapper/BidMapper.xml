<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ljh.sideproj.mapper.BidMapper">
    
    <insert id="insertBid" parameterType="Bid">
        INSERT INTO bids (user_id, pblnc_no, pblnc_nm, bid_amount, bid_date)
        VALUES (#{userId}, #{pblncNo}, #{pblncNm}, #{bidAmount}, NOW())
    </insert>
    
    <select id="findByUserId" parameterType="Long" resultType="Bid">
        SELECT bid_id, user_id, pblnc_no, pblnc_nm, bid_amount, bid_date
        FROM bids
        WHERE user_id = #{userId}
        ORDER BY bid_date DESC
    </select>
    
</mapper>